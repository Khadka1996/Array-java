public class ProperInitialization {
    public static void main(String[] args) {
        int[] numbers = new int[3]; // Initialized with default values (0, 0, 0)
        numbers[0] = 10;           // Assigning values
        System.out.println(numbers[0]); // Outputs: 10
    }
}
